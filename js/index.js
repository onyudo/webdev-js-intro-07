// Prevent us from attempting to use variables
// that are not declared
"use strict"

/*

// Create some functions, return a value and log that value to the console.

function getNumber() {
    const number = 42;
    console.log(number); 
    return number;       
  }
  
getNumber(); // call function. Output: 42
  
function getString() {
    const message = "Hello, World!";
    console.log(message);
    return message;
}

getString(); // call function. Output: Hello, World!

function getBoolean() {
    const isTrue = true;
    console.log(isTrue);
    return isTrue;
}

getBoolean(); // call function. Output: true

// Create some functions that take in arguments. Try to write at one least one that returns a value and one that doesn't.

function addNumbers(a, b) {
    return a + b;  // Returns the sum of a and b
}

let result = addNumbers(5, 3);
console.log(result);  // Logs 8 to the console

function printGreeting(name, age) {
    console.log(`Hello, ${name}! You are ${age} years old.`);
    // No return statement, so it implicitly returns undefined
}

// Create some functions that take in arguments, but also have default parameters.

function calculateArea(width = 5, height = 10) {
    return width * height;  // Returns the area of the rectangle
}

let area1 = calculateArea(8, 12);  // Uses provided values
console.log(area1);  // Logs 96

let area2 = calculateArea(8);  // Uses default height of 10
console.log(area2);  // Logs 80

let area3 = calculateArea();  // Uses default width of 5 and default height of 10
console.log(area3);  // Logs 50

*/



